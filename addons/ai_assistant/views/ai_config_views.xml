<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- AI Config Form View -->
    <record id="view_ai_config_form" model="ir.ui.view">
        <field name="name">ai.config.form</field>
        <field name="model">ai.config</field>
        <field name="arch" type="xml">
            <form string="Cấu hình AI">
                <header>
                    <button name="test_connection" type="object" string="🔍 Test kết nối" class="btn-primary" icon="fa-plug"/>
                    <field name="active" widget="boolean_toggle"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only"/>
                        <h1>
                            <field name="name" placeholder="Tên cấu hình..."/>
                        </h1>
                    </div>
                    <group>
                        <group string="🔑 API Configuration">
                            <field name="api_key" password="True" placeholder="sk-or-v1-..."/>
                            <field name="api_url" placeholder="https://openrouter.ai/api/v1/chat/completions"/>
                            <field name="model" placeholder="e.g., anthropic/claude-3.5-sonnet"/>
                        </group>
                        <group string="⚙️ Tham số AI">
                            <field name="max_tokens" placeholder="e.g., 4000"/>
                            <field name="temperature" widget="float" placeholder="0.0 - 1.0"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="📝 System Prompt" name="system_prompt">
                            <field name="system_prompt" nolabel="1" placeholder="Nhập prompt hệ thống mặc định để AI hiểu rõ vai trò và nhiệm vụ..."/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- AI Config Tree View -->
    <record id="view_ai_config_tree" model="ir.ui.view">
        <field name="name">ai.config.tree</field>
        <field name="model">ai.config</field>
        <field name="arch" type="xml">
            <tree string="Cấu hình AI">
                <field name="name"/>
                <field name="model"/>
                <field name="active"/>
            </tree>
        </field>
    </record>

    <!-- Action -->
    <record id="action_ai_config" model="ir.actions.act_window">
        <field name="name">Cấu hình AI</field>
        <field name="res_model">ai.config</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Thêm cấu hình AI
            </p>
            <p>
                Cấu hình kết nối với OpenRouter API để sử dụng tính năng AI.
            </p>
        </field>
    </record>
</odoo>
